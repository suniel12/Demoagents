# Core
anthropic>=0.42.0
httpx>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Agent framework
# Using raw Anthropic tool_use API — no framework dependency
# This is intentional: AgentCI should work with ANY framework,
# and dogfooding with raw API calls ensures we don't couple to LangChain/CrewAI

# Testing
agentci>=0.1.0        # Our own testing framework
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Utilities
rich>=13.0.0          # Pretty console output for reports
tenacity>=9.0.0       # Retry logic (Phase 3)
